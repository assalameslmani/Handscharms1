import React from "react";
import Header from "./../Components/Header";

const OrderScreen = () => {
    window.scrollTo(0, 0);

    return (
        <div>
            <Header/>
        </div>
        );
    };
    
    export default OrderScreen;